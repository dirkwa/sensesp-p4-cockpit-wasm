build/
# public/jlp_wasm.{js,wasm} are intentionally committed (see AGENTS.md):
# the designer pulls them without an emscripten toolchain. Keep them in
# sync after any firmware widget change.
src/lv_conf.firmware.h.ref
